name: Deploy Arzonic to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    env:
      APP_DIR: /home/arzonic/htdocs/arzonic.com
      PM2_NAME: arzonic-app
      PORT: 7000

    defaults:
      run:
        shell: bash

    steps:
      - name: 📥 Checkout repository into APP_DIR
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          path: ${{ env.APP_DIR }}

      - name: 🧰 Setup Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: 🧹 Clean previous Next.js build
        run: |
          cd "$APP_DIR"
          rm -rf .next .next/cache public/_next || true

      - name: 📦 Install dependencies
        run: |
          cd "$APP_DIR"
          npm ci

      - name: 🔇 Disable Next.js telemetry
        run: |
          cd "$APP_DIR"
          npx next telemetry disable

      - name: 🔧 Build Next.js project
        run: |
          cd "$APP_DIR"
          echo "⚡ Building Next.js app..."
          npm run build
          [ -d ".next" ] || { echo "❌ .next mangler"; exit 1; }

      - name: 🚀 Restart PM2 process
        run: |
          export PATH=$PATH:/home/arzonic/.npm-global/bin
          cd "$APP_DIR"
          pm2 delete "$PM2_NAME" || true
          PORT=$PORT pm2 start npm --name "$PM2_NAME" -- start
          pm2 save
          pm2 list

      - name: ✅ Deployment completed
        run: echo "🚀 Deployment successful!"
